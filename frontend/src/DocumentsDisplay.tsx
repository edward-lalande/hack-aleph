import React from 'react';
import { Box } from '@mui/material';

const DocumentsDisplay:React.FC = () => {
    return (
    <Box sx={{display: "flex"}}>

    </Box>);
};

export default DocumentsDisplay;
